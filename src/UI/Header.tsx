import React from 'react';

const Header: React.FC = () => {
	return (
		<header className="header">
			<h1 className="title header__title">Почасовая температура</h1>
		</header>
	);
};

export default Header;
