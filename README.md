# **Тестовое задание ПАО «Татнефть»**

## 🌤️ **Weather Charts**

### Приложение для визуализации погодных данных в реальном времени

### 🚀 **Быстрый старт**

```bash
# 1. Клонируем и заходим
git clone https://github.com/Alaicry/tatneft-test-task.git

cd tatneft-test-task

# 2. Устанавливаем зависимости
yarn (или npm install)

# 3. Запускаем dev-сервер
yarn dev (или npm run dev)
# ➜ http://localhost:3000
```

🛠 Технологии

-   React 18 + TypeScript

-   Redux Toolkit для управления состоянием

-   Chart.js с react-chartjs-2 для графиков

-   SCSS для стилей с CSS-переменными

-   Vite для сборки

📝 Скрипты

```
yarn dev (или npm run dev) - запуск dev сервера

yarn build (или npm run build) - сборка для production

yarn preview (или npm run preview) - предпросмотр сборки

yarn lint (или npm run lint) - форматирование кода Eslint
```
